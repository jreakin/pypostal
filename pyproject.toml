[project]
name = "postal"
dynamic = ["version"]
requires-python = ">=3.7"
dependencies = ["six"]

[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[tool.uv]
dev-dependencies = [
    "nose>=1.0",
    "pytest>=6.0",
    "build",
]
